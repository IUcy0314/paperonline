请根据项目中的 `questions.js` 搭建一个本地 HTML 离线版刷题软件。

核心需求：

1. 项目文件夹已经建好，名称为“刷题软件”。
2. 项目中已经有 `questions.js`。
3. 题库没有正确答案，不需要 `answer` 字段。
4. 题库不需要解析，不需要 `explanation` 字段。
5. 需要生成 `index.html`。
6. 用户双击 `index.html` 后即可离线刷题。
7. 不需要服务器、不需要数据库、不需要联网、不需要 npm、不需要 Vue/React。
8. 使用原生 HTML + CSS + JavaScript 实现。

最重要的逻辑：

第一次做题时进入“答案录入模式”，用户提交的答案会被保存为该题的标准答案。
之后进入“顺序刷题模式”或“随机刷题模式”，系统按照第一次保存的答案判分。
答错的题自动加入错题本，错题本可单独练习。

需要支持的模式：

* 答案录入模式
* 顺序刷题模式
* 随机刷题模式
* 错题练习模式

需要支持的按钮：

* 提交
* 上一题
* 下一题
* 清空错题
* 清空已录入答案

本地保存内容：

* `savedAnswers`：保存用户第一次录入的标准答案
* `wrongQuestions`：保存错题
* `currentIndex`：保存当前进度，可选

请不要修改 `questions.js` 的基本结构，也不要强制要求题库中有答案字段。项目重点是：题库本身没有答案，用户第一次做题的答案就是之后判分用的标准答案。
:::
